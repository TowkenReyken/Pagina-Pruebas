<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Editar Publicación</title>
</head>
<body>
    <header>Editar Publicación</header>
    <div class="container">
        <form action="/post/<%= post._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
            <label for="title">Título:</label>
            <input type="text" name="title" id="title" value="<%= post.title %>" required>
            
            <label for="content">Contenido:</label>
            <textarea name="content" id="content" rows="4" required><%= post.content %></textarea>
            
            <label for="image">Imagen:</label>
            <input type="file" name="image" id="image" accept="image/*">
            <% if (post.image) { %>
                <img src="/<%= post.image %>" alt="Imagen de la publicación" style="max-width: 300px;">
            <% } %>
            
            <button type="submit">Guardar Cambios</button>
        </form>
    </div>
    <footer>© 2024 Blog Personal</footer>
</body>
</html>